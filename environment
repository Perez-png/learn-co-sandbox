<<<<<<< HEAD
Blueprint for my project includes


basic plan for project....
model
Scraper
results are on other program 
cannot find reposirtory with original data.
Thank you for this opportunity


=======
>>>>>>> a2299caa9c481d54d45698c9282a3f5a75f20a29
require '...lib/environment.rb
require bundler
require pry

class mycliproject

end



class mycliProject
    attr_accessor :name, location, url, availability
        @@all = []
end
    def initialize (0)
        @name = name
        @project = project
        @url = url
        @availability = availability
        @@all << all

    end
def self.all
    @self 
 end

def menu.new
    puts input = nil
    input = get.strip
    case input
        when "1"
            puts"more info on page 1"
        when "2"
            puts""more info on page 2"
        when list__list details
def goodbye
    puts " See you next time"
    end
else "Not sure what you want, type1,2,3, or exit"

    end
 

 def self.new_from_google(url)
    end

end


array_of_links.map do |link|
    binding.pry
    MyCLIProject::Catagory.new(lnk.text, lnk, attributes,href, value)

    section.css(text url and open description of object)
    section.css(text url and open description of object)
    section.css(text url and open description of object)
    section.css(text url and open description of object)


##Scraper.scrape_index_page(index_url)
##Scraper.scrape_profile_page(profile_url)



MyCLIProject.self 
Scraper.new 
Scraper.index_url
Scraper.profile_url
@all << self 


    end
end


end
  
